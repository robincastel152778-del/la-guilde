name: Réveil du serveur
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping La Guilde
        run: curl -s -o /dev/null "https://la-guilde-pondy.onrender.com/" || true
